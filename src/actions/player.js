import { createAction } from '@reduxjs/toolkit';

export const setItem = createAction('player/setItem');
export const play = createAction('player/play');
export const pause = createAction('player/pause');
